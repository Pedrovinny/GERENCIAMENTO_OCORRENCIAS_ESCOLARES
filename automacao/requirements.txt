# Dependências extras dos robôs locais (automacao/), além do requirements.txt
# da raiz do projeto. botcity-framework-core traz pywin32/pywinauto/comtypes,
# que são exclusivos do Windows — por isso NÃO fica no requirements.txt raiz
# (que também é usado para instalar o app Django num servidor Linux).
-r ../requirements.txt
botcity-framework-core==1.3.0
